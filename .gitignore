# Dépendances
node_modules/

# Claude Code — réglages locaux, non versionnés
.claude/settings.local.json

# Sorties d'inspection éphémères (screenshots de travail)
.shots/
demos/**/.shots/

# Cache des outils
**/.vite/
*.tsbuildinfo

# OS / logs
.DS_Store
Thumbs.db
*.log
.env
.env.local

# IMPORTANT : on COMMIT public/ (le dist statique servi par le serveur)
# et registry.json (l'index des démos). Ne pas les ignorer.
